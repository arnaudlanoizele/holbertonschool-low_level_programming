#include "binary_trees"

/**
* binary_tree_insert_right - Insert a nodes ax the right-child of another node
*
* @parent: A pointer to the node to insert the right-child
* @value: The value to store in the new node
*
* Return: If parent is NULL or an error occure - NULL
* 			Otherwise - apointer to the new node
*
* Description: If parent already has a right-child, the new node takes its place
* 			and the old right-child is set as the right-child of the new node
*/
binary_tree_t *binary_tree_insert_right(binary_tree_t *parent, int value)
{

}
